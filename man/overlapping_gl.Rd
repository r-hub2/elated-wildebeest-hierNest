% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparsegl.R
\name{overlapping_gl}
\alias{overlapping_gl}
\title{Fit an Overlapping Group Lasso Model}
\usage{
overlapping_gl(
  x,
  y,
  group = NULL,
  family = c("gaussian", "binomial"),
  nlambda = 100,
  lambda.factor = ifelse(nobs < nvars, 0.01, 1e-04),
  lambda = NULL,
  pf_group = sqrt(bs),
  pf_sparse = rep(1, nvars),
  intercept = TRUE,
  asparse1 = 1,
  asparse2 = 0.05,
  standardize = TRUE,
  lower_bnd = -Inf,
  upper_bnd = Inf,
  weights = NULL,
  offset = NULL,
  warm = NULL,
  trace_it = 0,
  dfmax = as.integer(max(group)) + 1L,
  pmax = min(dfmax * 1.2, as.integer(max(group))),
  eps = 1e-08,
  maxit = 3e+06,
  cn,
  drgix,
  drgiy,
  cn_s,
  cn_e,
  random_asparse = FALSE
)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (no missing values allowed).}

\item{y}{A numeric vector of response variable values.}

\item{group}{An integer vector defining the group membership for each predictor. Default is NULL (each predictor forms its own group).}

\item{family}{A character string specifying the model family. Options are "gaussian" (default) and "binomial".}

\item{nlambda}{Number of lambda values. Default is 100.}

\item{lambda.factor}{Factor determining minimum lambda as a fraction of maximum lambda. Default depends on dimensionality.}

\item{lambda}{Numeric vector of lambda values. If provided, overrides `nlambda` and `lambda.factor`.}

\item{pf_group}{Penalty factor for groups. Default is square root of group size.}

\item{pf_sparse}{Penalty factor for individual predictors. Default is 1 for each predictor.}

\item{intercept}{Logical; whether to include an intercept. Default is TRUE.}

\item{asparse1}{Sparsity penalty factor controlling group-level sparsity. Default is 1.}

\item{asparse2}{Sparsity penalty factor controlling within-group sparsity. Default is 0.05.}

\item{standardize}{Logical; if TRUE, standardizes predictors before fitting. Default is TRUE.}

\item{lower_bnd}{Numeric vector specifying lower bounds for coefficients. Default is -Inf.}

\item{upper_bnd}{Numeric vector specifying upper bounds for coefficients. Default is Inf.}

\item{weights}{Optional numeric vector of observation weights. Currently limited functionality.}

\item{offset}{Optional numeric vector specifying a known component to be included in the linear predictor.}

\item{warm}{Optional initial values for optimization.}

\item{trace_it}{Integer indicating the verbosity level. Default is 0 (no output).}

\item{dfmax}{Maximum number of groups allowed in the model. Default derived from groups.}

\item{pmax}{Maximum number of predictors allowed in the model. Default derived from groups.}

\item{eps}{Numeric convergence threshold for optimization. Default is 1e-08.}

\item{maxit}{Maximum number of iterations for optimization. Default is 3e+06.}

\item{cn}{Additional internal numeric parameter for optimization.}

\item{drgix, drgiy}{Numeric vectors specifying indices for specific group and predictor structures.}

\item{cn_s, cn_e}{Numeric vectors specifying starting and ending indices for substructures.}

\item{random_asparse}{Logical; if TRUE, randomly selects sparsity parameters. Default is FALSE.}
}
\value{
An object of class `sparsegl` containing:
\item{call}{The matched function call.}
\item{lambda}{The lambda values used for fitting.}
\item{asparse1, asparse2}{Sparsity parameters used.}
\item{nobs}{Number of observations.}
\item{pf_group, pf_sparse}{Penalty factors used.}
\item{coefficients}{Estimated coefficients.}
Additional components relevant to model diagnostics and fitting.
}
\description{
This function fits an overlapping group lasso model with hierarchical regularization, allowing both sparsity within groups and across groups.
}
